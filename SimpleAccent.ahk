^a::
    if (GetKeyState("Capslock", "T"))
        send, Á
    else
        send, á
return

^e::
    if (GetKeyState("Capslock", "T"))
        send, É
    else
        send, é
return

^i::
    if (GetKeyState("Capslock", "T"))
        send, Í
    else
        send, í
return

^o::
    if (GetKeyState("Capslock", "T"))
        send, Ó
    else
        send, ó
return

^u::
    if (GetKeyState("Capslock", "T"))
        send, Ú
    else
        send, ú
return

^;::
    if (GetKeyState("Capslock", "T"))
        send, Ñ
    else
        send, ñ
return